@@ -8,6 +8,8 @@ def save_patch(filename: str, patch: str):
 
     file_path = reviews_dir / f"{safe_name}.diff"
 
+    print(f"Saving patch to reviews/: {filename} -> {file_path}")
+
     with open(file_path, "w", encoding="utf-8") as file:
         file.write(patch)
 